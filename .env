# Default API base URL for local development. Committed since it's not secret (Expo
# bundles EXPO_PUBLIC_* vars into the client regardless), but a real deployment should
EXPO_PUBLIC_API_BASE=https://api.scancode.ng
